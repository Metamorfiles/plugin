{
  "mcpServers": {
    "metamorfiles": {
      "command": "npx",
      "args": [
        "-y",
        "--fetch-retries=0",
        "--fetch-timeout=5000",
        "metamorfiles@latest",
        "mcp",
        "--plugin-root",
        "${CLAUDE_PLUGIN_ROOT}"
      ]
    }
  }
}
